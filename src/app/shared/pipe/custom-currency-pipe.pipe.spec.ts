import { CustomCurrencyPipePipe } from './custom-currency-pipe.pipe';

describe('CustomCurrencyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomCurrencyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
